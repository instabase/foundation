syntax = "proto2";

package foundation;

import "foundation/proto/geometry.proto";

message TargetValue {
  optional string id = 1;
  optional string field_name = 2;
  optional BBox bbox = 3;
  optional string value = 4;
}

message TargetValueCollection {
  optional string id = 1;
  repeated string target_value_ids = 2;
}